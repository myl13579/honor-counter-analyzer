# 自主规划分析 Agent 升级 — 开发计划文档

> 项目：honor-counter-analyzer（王者对局分析 Agent）
> 目标：将现有「单次 query 流式」升级为 **LangGraph 规划-执行-反思 完整自治循环 + 浏览器联网检索**，底层 LLM 替换为 DeepSeek，扩展对局内信息问答能力，并加入超时/重试安全约束。
> 文档存放位：本文件位于项目根目录 `honor-counter-analyzer/自主规划循环-开发计划.md`

---

## 一、项目名称与目标

- **名称**：honor-counter-analyzer「自主规划 + 联网检索」架构升级
- **目标**：
  1. 用 LangGraph（JS 版 `@langchain/langgraph`）实现 `Planner → Executor → Critic` 闭环，Agent 自主拆解步骤、多轮调用工具、反思不足时重规划。
  2. 底层 LLM 切换为 DeepSeek（`@langchain/deepseek`，`deepseek-chat`），替换 `@tencent-ai/agent-sdk`。
  3. **领域扩展**：从「克制关系」扩展到对局内信息问答——英雄打法思路与对线策略、局内出装推荐、根据敌方出装动态推荐出装。
  4. **联网检索**：知识库无答案时，自动调用 DuckDuckGo 联网检索，结果交 LLM 整合后输出。
  5. **安全约束**：单轮超时 1 分钟注入「换个思路」提示词；重试上限 3 次，超过终止并返回提示。
  6. 保留「无 API key 也能跑通」的 demo 降级；复用现有知识库与富元素渲染协议。

---

## 二、功能需求清单（优先级排序）

| 优先级 | 功能 | 说明 |
|---|---|---|
| P0 | LangGraph 规划-执行-反思图 | StateGraph：planner / executor / critic / synthesizer + 条件边闭环 |
| P0 | DeepSeek LLM 接入 | `@langchain/deepseek` 的 `ChatDeepSeek`，`DEEPSEEK_API_KEY` 注入 |
| P0 | 知识库工具（Read/Grep） | 读 `counters.md`/`heroes.json`/`items.json`/`summoners.json`；Grep 模糊搜索英雄名 |
| P0 | **联网检索工具（WebSearch）** | 封装 DuckDuckGo 检索；知识库无答案时自动触发，结果交 LLM 整合 |
| P0 | SSE 流式对接 | LangGraph `.stream()` 映射为 SSE（`plan`/`tool`/`text`/`done`） |
| P0 | demo 降级保留 | 无 key 走 `demoService.ts` 规则引擎，有 key 走 LangGraph 循环 |
| P0 | **超时机制** | 单轮超 60s 中断，注入「当前思路可能出现问题，可以换个思路思考」 |
| P0 | **重试上限** | 超时/重规划最多 3 次，超过终止并返回「任务过于复杂」提示 |
| P1 | 领域知识扩充 | 新增 `playstyle.md`（打法/对线）、扩展 `builds`（局内出装） |
| P1 | 动态出装推荐 | 用户输入敌方英雄及出装 → 推荐克制出装（预置 + 联网兜底） |
| P1 | 循环上限 `max_iterations` | 防止死循环 |
| P1 | 前端规划可视化 | 计划卡片 + 步骤勾选 + 工具时间线（含联网工具展示） |
| P1 | 工具调用持久化 | 落库 `tools` 字段，刷新可恢复 |
| P2 | 移除旧 SDK | 删 `agentService.ts` 与 `@tencent-ai/agent-sdk` |

---

## 三、技术方案与依赖项

### 3.1 架构（StateGraph）

```
START → planner ──(生成 plan JSON)──→ executor ──(Read/Grep/WebSearch)──→ critic
                                        ↑                                        │
                                        └──────── continue（下一步）──────────────┤
                                        └──────── replan（换思路重规划）────────────┤
                                                                        │ finish / retry 超限
                                                                        ▼
                                                                  synthesizer → END
```

**State（Annotation）**：

```ts
{
  messages: 消息累积（reducer 累加）
  plan: { steps: { goal: string; tool?: 'read'|'grep'|'web' }[] }
  stepIndex: number
  observations: string[]       // 工具结果
  iterations: number           // 循环计数
  retryCount: number           // 超时/重规划计数（安全约束）
  timeoutFlag: boolean         // 是否发生超时
  finalAnswer: string
}
```

**节点职责**：

| 节点 | 职责 |
|---|---|
| `planner` | 调 LLM 拆解用户目标为步骤清单，识别是否需要联网 |
| `executor` | 执行当前步骤，调用 Read/Grep/WebSearch，结果写入 `observations` |
| `critic` | 评估结果是否充分；检测超时则注入换思路提示词；输出 `continue`/`replan`/`finish` |
| `synthesizer` | 汇总结果，产出最终分析（复用富元素标记） |

**条件边**：`critic` → `continue`→executor / `replan`→planner（`retryCount+1`）/ `finish`→synthesizer；`retryCount ≥ 3` 或 `iterations ≥ max` 强制 `finish`。

### 3.2 安全约束实现

1. **超时 60s**：每个 LLM 调用节点用 `AbortController` + `Promise.race` 加 60s 超时。超时中断当前调用，置 `timeoutFlag=true`，critic 检测到后注入提示词「当前思路可能出现问题，可以换个思路思考」，路由 `replan`。
2. **重试 3 次**：`replan` 时 `retryCount+1`；`retryCount ≥ 3` 时 critic 强制 `finish`，synthesizer 返回「任务过于复杂，请简化问题或分步描述」。

### 3.3 联网检索设计

- 工具：`WebSearch`，封装 DuckDuckGo 检索（优先 Instant Answer API `api.duckduckgo.com`，无结果时退 HTML 搜索端点），返回标题 + 摘要。
- **触发条件**：知识库 Read/Grep 未命中，或 critic 判定知识库信息不足时，planner 在计划中标记该步骤 `tool:'web'`。
- 抽象 `SearchProvider` 接口，便于后续替换 Tavily/国内 API（DuckDuckGo 国内直连不稳的备选）。
- 联网结果作为 observation 交 LLM 整合，遵循「检索结果 + 知识库」共同产出，避免编造。

### 3.4 依赖项（server 新增）

```
@langchain/langgraph   # StateGraph / Annotation / START / END / MemorySaver
@langchain/core        # tool / HumanMessage / AIMessage
@langchain/deepseek    # ChatDeepSeek（deepseek-chat，支持 Tool Calling）
zod                    # tool schema 定义
```

移除：`@tencent-ai/agent-sdk`。

### 3.5 关键实现路径

1. `server/src/agent/langgraphService.ts`（新增）：State、四节点、图编译、超时/重试逻辑；暴露 `runGraph(prompt)` 异步生成器 yield `{type:'plan'|'tool'|'text'|'done'}`。
2. `server/src/agent/tools.ts`（新增）：`ReadKnowledge` / `GrepKnowledge` / `WebSearch` 三个 LangChain tool。
3. `server/src/agent/searchProvider.ts`（新增）：`SearchProvider` 接口 + DuckDuckGo 实现。
4. `server/src/routes/chat.ts`（改）：有 `DEEPSEEK_API_KEY` 走 LangGraph，否则 demo；SSE 透传 plan/tool/text/done。
5. `server/src/config.ts`（改）：`DEEPSEEK_API_KEY` 检测 + 超时/重试参数常量。
6. `data/knowledge/playstyle.md`（新增）：英雄打法思路与对线策略（先覆盖热门英雄，逐步补齐）。
7. `web/src/components/ChatPanel.tsx`（改）：计划卡片、步骤状态、工具时间线（含联网工具）。
8. `server/src/agent/systemPrompt.ts`（改）：拆为 planner/critic/synthesizer 三段提示词，明确「知识库优先，无答案再联网」。

---

## 四、测试用例

| 编号 | 输入 | 预期输出 | 边界条件 |
|---|---|---|---|
| T1 | 有 key，输入「后羿」 | planner 拆步 → executor 读知识库 → critic finish → synthesizer 输出 counter+技能+出装 | 计划非空、工具 ≥1 次 |
| T2 | 无 key | 走 demo 规则引擎，SSE 含 `meta.degraded` | 不调 DeepSeek |
| T3 | 「后羿打法思路与对线策略」 | 知识库 `playstyle.md` 命中则直接答；未命中触发 WebSearch 联网整合 | 联网结果有据可查 |
| T4 | 「敌方后羿出了破晓，我该出什么」 | 用户提供敌方出装 → 推荐克制出装 | 动态出装数据流正确 |
| T5 | 「赛文是谁」（未知英雄） | 知识库无 → 联网检索 → 仍无则提示「未找到」，不编造 | 不误报 |
| T6 | 构造单轮超 60s | 中断 + 注入换思路提示词 + `retryCount+1` | 超时生效 |
| T7 | 构造连续 3 次超时/重规划 | `retryCount≥3` 终止，返回「任务过于复杂」 | 不死循环 |
| T8 | 空输入 / 超长输入 | 空输入 400；超长正常或优雅降级 | 边界不崩溃 |
| T9 | 刷新会话 | 工具调用记录与消息一起恢复 | `tools` 已落库 |
| T10 | DuckDuckGo 不可用 | WebSearch 返回空 → 降级为「仅知识库回答」+ 提示 | 不崩溃 |

---

## 五、里程碑与验收标准

| 里程碑 | 验收标准 |
|---|---|
| M1 依赖接入 | 安装 langgraph/langchain-core/langchain-deepseek/zod 成功；`ChatDeepSeek` 真实 key 冒烟连通 |
| M2 图跑通 | T1 走完 planner→executor→critic→synthesizer 全链路 |
| M3 工具集 | Read/Grep/WebSearch 三个工具可被 executor 正确调用 |
| M4 联网检索 | T3 知识库未命中时自动联网并整合；T10 联网不可用时不崩溃 |
| M5 安全约束 | T6 超时注入、T7 重试 3 次终止均生效 |
| M6 流式 + 前端 | SSE 顺序正确，计划卡片 + 工具时间线渲染正确 |
| M7 demo 降级 | 无 key T2 正常，有 key T1 正常，切换无副作用 |
| M8 边界回归 | T3–T10 全过；`npm run build -w server` / `-w web` 通过；既有安全/性能修复不回退 |
| M9 提交 | 本地 Git 提交，README/DEVELOPMENT.md 更新运行方式与架构 |

---

## 六、风险与备选

1. **JS 版 LangGraph 生态弱于 Python**：文档样例以 Python 为主。备选：改用 Python FastAPI 独立服务承载 LangGraph。当前按 JS 版推进。
2. **DuckDuckGo 国内直连不稳**：`SearchProvider` 接口已抽象，可无缝替换 Tavily/国内 API（需 key）。
3. **DeepSeek 仅 `deepseek-chat` 支持 Tool Calling**：禁用 `deepseek-reasoner`。
4. **联网结果质量与时效**：王者版本迭代快，联网结果可能过时/错误，需 synthesizer 声明「联网信息仅供参考，以游戏内版本为准」。
5. **超时粒度**：60s 超时若过严可能误伤正常长任务，M5 阶段按实测调优（超时阈值可配）。
6. **API key 安全**：`DEEPSEEK_API_KEY` 仅走环境变量，`.env` 已忽略，不得硬编码。

---

## 七、文档存放位

- 本计划：`honor-counter-analyzer/自主规划循环-开发计划.md`
- 实现后同步更新：`README.md`（运行方式/架构/联网说明）、`DEVELOPMENT.md`（双模式 → 三模式）
- 领域知识新增：`data/knowledge/playstyle.md`
